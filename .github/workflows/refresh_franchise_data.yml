name: Refresh Franchise Data

on:
  schedule:
    # Tuesday and Thursday mornings. GitHub Actions cron always runs in UTC
    # (there's no named-timezone option), so 12:00 UTC lands around 7-8am
    # US Eastern depending on daylight saving — adjust the hour here if you
    # want it to land at a more precise local time.
    - cron: '0 12 * * 2,4'
  workflow_dispatch: # Manual button inside GitHub web console

permissions:
  contents: write

jobs:
  build-and-commit:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install System Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install gspread google-auth

    - name: Create Google Secret File
      run: echo '${{ secrets.GOOGLE_CREDENTIALS }}' > google_secret.json

    - name: Run Franchise Data Script
      env:
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: f53f8e388e691eca52769f9f586713df
      run: python generate_franchise_data_d1.py

    - name: Run Draft Board Data Script
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: f53f8e388e691eca52769f9f586713df
      run: python generate_draft_board_data.py

    - name: Run Rule Changes Data Script
      env:
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
      run: python generate_rule_changes_data.py

    - name: Remove Google Secret File
      if: always()
      run: rm -f google_secret.json

    - name: Commit and Push Updated Data
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add docs/data/franchise.json docs/data/draft-board.json docs/data/rule-changes.json
        git diff --cached --quiet || git commit -m "Update franchise, draft board, and rule changes data"
        git push
